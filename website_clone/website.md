Сразу в дело. Давай спроектируем **полную структуру продукта**: от лендинга до самой глубокой вкладки клона.

---

## A. Публичная главная (лендинг до логина)

**Цель:** показать уровень, а не «ещё один чат-бот-конструктор».

### 1. Hero-блок (первый экран)

* Слева:

  * Большой заголовок уровня:
    **«Личные AI-двойники, а не просто боты»**
  * Подзаголовок:
    «Загрузи реальные диалоги — получи клона, который ведёт себя как человек».
  * Кнопки:

    * `Создать первого клона` (primary → ведёт в регистрацию/онбординг).
    * `Посмотреть демо` (secondary → открывает модальное окно с живой демо-сессией клона).
* Справа:

  * Интерактивный mock: окно диалога с «клон-профилем» (мини UI), где показываются:

    * Аватар человека
    * Плашка: «Синхронизация с новым диалогом…»
    * 2–3 анимированных сообщения, показывающих «человечность».

Ниже в Hero — мелкие элементы:

* `⚙ 100% локальный inference у клиента`
* `🔐 Данные — только ваши`
* `🧠 Специализация: реалистичное поведение, а не просто ответы`

### 2. Блок «Как это работает» (3 шага)

В виде карточек/иконок:

1. **Загрузи диалоги**

   * Источники: Telegram, WhatsApp, e-mail, txt/pdf.
2. **Настрой поведение**

   * Слайдеры и правила: стиль общения, границы, триггеры.
3. **Разверни клона**

   * Кнопки: «в Телеграм», «в WhatsApp», «в локальную ноду».

### 3. Блок «Для кого»

3 колонки:

* **Создатели личных ассистентов / инфлюенсеры**
* **HR / обучение / внутренние эксперты**
* **Фаундеры и агент-платформы (твоя основная аудитория)**

### 4. Блок «Локальные ноды / On-Prem»

Карта:
`Обучение в облаке → Пакет модели → Локальная нода у клиента`.
Кнопка: **Документация по локальному развёртыванию** (ведёт к твоей уже готовой веб-панели).

### 5. CTA внизу

«Создай первого клона за 10 минут без кода» → кнопка `Начать`.

---

## B. Онбординг после регистрации

Сразу показываем, что это не обычный SaaS.

1. Экран 1: «Что ты хочешь клонировать?»

   * Карточки:

     * `Я → личный ассистент`
     * `Эксперт / наставник`
     * `Саппорт-агент`
     * `Другой профиль (кастом)`
   * Внизу: чекбокс «У меня есть готовые диалоги».

2. Экран 2: «Подключи данные»

   * Кнопки-источники:

     * `Импорт Telegram .json/.html`
     * `Импорт WhatsApp .txt`
     * `Загрузить файлы (txt/pdf/docx)`
     * `Подключить API (для продвинутых)`
   * Статус-бар «Качество датасета» (будет считаться позже).

3. Экран 3: «Быстрая персонализация»

   * Поля:

     * Имя клона
     * Краткое описание («Кто это?»)
   * 3 слайдера:

     * `Насколько эмоционален`
     * `Насколько прямолинеен`
     * `Насколько кратко отвечает`
   * Снизу: `Продвинутые настройки →` (ведёт в уже основной интерфейс клона).

4. Экран 4: «Готово — запустить первую тренировку»

   * Кнопка `Запустить обучение`
   * Чекбокс `Открыть клон в песочнице после обучения`.

---

## C. Главный экран личного кабинета (Dashboard)

После логина пользователь попадает сюда.

### Верхняя панель

* Лого стартапа + название платформы
* Вправо:

  * `Документация`
  * `Сообщить об идее / баге`
  * Иконка профиля (аккаунт, биллинг, API-ключи)

### Левая навигация (основные разделы)

1. `Клоны`
2. `Проекты` (группировка клонов/датасетов под конкретные кейсы)
3. `Датасеты`
4. `Обучение` (Training Jobs / очереди)
5. `Тесты и Эвалюации`
6. `Деплой и интеграции`
7. `Настройки команды` (если план Pro/Team)

### Центральная часть Dashboard

#### 1. «Мои клоны» (основной блок)

Сетка карточек:

Каждая карточка клона содержит:

* Аватар
* Имя
* Тэг (тип: «личный ассистент / эксперт / саппорт»)
* Статусы:

  * `Обучен до: 23.11.2025`
  * `Последний датасет: Telegram 2023–2025`
  * `Статус: Готов / Требует обновления`
* Быстрые действия:

  * `Открыть студию клона`
  * `Начать чат`
  * `Обновить на новых данных`
  * `Экспорт в локальную ноду`

Сверху справа: кнопка `Создать клона`.

#### 2. Блок «Очередь обучения»

Мини-таблица:

* Клон | Статус | Прогресс | ETA | Кнопка «Подробнее»
* Вкл/выкл автorefresh.

#### 3. Блок «Качество и риски»

* Виджет:

  * `Средний score реалистичности` (по тестам)
  * `Покрытие сценариев` (сколько сценариев протестировано)
  * `Кол-во нарушений ограничений` (если есть).

Цель: показать, что это серьёзный инструмент с эвалюацией, а не «загрузил и забыл».

---

## D. Структура навигации в личном кабинете

Предлагаю такую логическую структуру:

1. **Клоны**

   * Список
   * Карточка/Workspace конкретного клона (подробно ниже)

2. **Проекты**

   * Группировка клонов + датасетов + интеграций под один use-case.
   * Например: Проект «Customer Support RU» → 3 клон-агента + 2 датасета + 1 интеграция с Zendesk.

3. **Датасеты**

   * Каталог всех загруженных наборов данных.
   * Метаданные: источник, размер, язык, качество.

4. **Обучение**

   * История и очередь training jobs.
   * Логи, параметры, версии модели.

5. **Тесты и Эвалюации**

   * Наборы тестовых сценариев.
   * Метрики по каждому клону.

6. **Деплой и интеграции**

   * Выгрузка модели/адаптера.
   * Конфиг для Telegram/Slack/WhatsApp, Webhook/API.
   * Подключение локальных нод: список нод клиентов, статус, версия модели.

7. **Настройки**

   * Биллинг, команда, роли.
   * API-ключи.
   * Brand-настройки (логотип клиента и т.д.).

---

## E. Экран конкретного клона (workspace клона)

Это сердце продукта. Тут нужно сделать **необычно**.

### Верхняя панель клона

Слева:

* Аватар клона
* Имя + бейдж «Тип» (личный, эксперт, саппорт)
* Мини-описание («Клон Петра: предприниматель в крипте, любит короткие ответы»)

Справа:

* Статус: `Готов / Обучается / Требует дообучения`
* Версия модели: `v1.2.3`
* Кнопки:

  * `Запустить чат`
  * `Обновить на новых данных`
  * `Экспортировать`

### Вкладки клона (основные)

1. `Обзор`
2. `Данные и память`
3. `Поведение и границы`
4. `Тесты и сценарии`
5. `Деплой`
6. `Логи и версия`

Разберём по вкладкам.

---

### 1) Вкладка «Обзор»

Блоки:

* **Карта личности**
  Визуальный радар/паук со шкалами:

  * Эмоциональность
  * Формальность
  * Дружелюбие
  * Решительность
  * Склонность к юмору
    Все параметры вычисляются автоматически из датасета, но можно слегка корректировать.

* **Источники данных**
  Список:

  * Telegram export 2022–2024 (35k сообщений)
  * WhatsApp с мамой 😄 (5k сообщений)
  * PDF: «Посты в телеграм-канале» (100 страниц)

* **Статус обучения**

  * Последняя тренировка: дата, время
  * Параметры (тип модели, LoRA/адаптер, шаги)
  * Кнопка `Посмотреть детали` (ведёт во вкладку «Логи и версия»).

* **Краткий лог тестов**

  * Последние тесты: `«Отвечает ли так же, как оригинал в конфликтной ситуации?»` → 87%
  * `«Использует ли сленг, как в исходных диалогах?»` → 92%

---

### 2) Вкладка «Данные и память»

Цель: дать **no-code** контроль над датасетом.

Блоки:

1. **Источники** (таб-view)

   * Таблица: Источник | Тип | Размер | Диапазон дат | Статус очистки | Кнопки:

     * `Открыть`
     * `Исключить из обучения`
     * `Обновить`

2. **Граф памяти**

   * Визуальный граф (узлы: темы, люди, проекты).
   * Можно кликнуть на «Мама», «Бизнес», «Универ» и увидеть, какие диалоги входят.
   * Возможность отключить часть памяти: «Не хочу, чтобы клон говорил про универ» → переключатель.

3. **Фильтрация и анонимизация (no-code)**

   * Простые правила как «блок-схемы»:

     * Если сообщение содержит `+998` или `@username` → замаскировать.
     * Если диалог с контактом «X» → не использовать.
   * Кнопка `Просмотреть примеры до/после`.

4. **Ручное редактирование**

   * Поиск по датасету (как поиск по чату).
   * Возможность:

     * убрать конкретные фрагменты,
     * пометить как «золотой эталон» (для тестов).

---

### 3) Вкладка «Поведение и границы»

Тут главная **фишка no-code платформы**.

#### Верх: Профиль поведения

* Слайдеры:

  * `Степень откровенности`
  * `Частота инициативы (сам пишет?)`
  * `Длина ответа`
  * `Толерантность к мату/сарказму`
* Переключатели:

  * `Всегда сохраняет вежливость`
  * `Не обсуждает политику`
  * `Не даёт финансовых советов`

#### Средняя часть: «Правила и триггеры» (визуальный редактор)

No-code интерфейс в стиле блок-схем:

* Блок `Если`:

  * «Пользователь спрашивает про деньги / доходы / заработок»
* Блок `Тогда`:

  * Использовать такой-то тон + добавить дисклеймер.

Примеры:

* Если в запросе «инвестиции», ответ начинается с «Я не даю финансовых советов, но могу поделиться опытом…»
* Если пользователь пишет в 3 ночи (по локальному времени), клон отвечает: «Эй, ты поздно ещё не спишь 😅»

#### Низ: «Примеры поведения»

* Таблица: Сценарий → Пример ответа клона → Кнопка `Посмотреть, как бы ответил оригинал` (если есть пример в датасете → помогает оценить, насколько клон отличается от реального человека).

---

### 4) Вкладка «Тесты и сценарии»

Тут ты показываешь серьёзный уровень как платформа для **эвалюации**, а не просто тренировки.

Блоки:

1. **Наборы тестов**

   * `Small talk`
   * `Конфликтные ситуации`
   * `Деньги и работа`
   * `Личные темы`
   * `Технические вопросы`
   * Каждый набор: метрика «насколько клон ведёт себя как оригинал».

2. **Сценарный тест (диалог)**

   * Последовательность сообщений → предустановленный сценарий.
   * Кнопка `Прогнать сценарий` → показывается диалог клона.
   * Возможно добавить ручную оценку: «Похож / не похож» (для тонкой калибровки).

3. **Авто-проверки границ**

   * Чек-лист: `Не нарушает запреты на деньги`, `Не раскрывает личные данные`, и т.п.
   * Кнопка `Запустить проверку`.

---

### 5) Вкладка «Деплой»

Разделение на **каналы** и **артефакты модели**.

1. **Каналы:**

   * Telegram
   * WhatsApp
   * Web-виджет
   * API

У каждого канала:

* Статус: не подключён / тест / прод.
* Кнопка `Сгенерировать инструкцию` (под конкретный бот-фреймворк: aiogram, pyrogram и т.д.).
* Кнопка `Проверить соединение`.

2. **Локальная нода / On-Prem**

* Список доступных нод (если клиент их установил):

  * `Node #1 — офис Москва`
  * `Node #2 — ноутбук CTO`
* Для каждой:

  * Версия модели, дата последнего обновления.
  * Кнопка `Выгрузить модель на ноду`.
  * Статус: `Синхронизировано / Требует обновления`.

3. **Экспорт модели**

* Кнопки:

  * `Экспорт LoRA / адаптера`
  * `Экспорт конфигов (YAML/JSON)` для твоей локальной веб-панели
  * `Экспорт RAG-пакета` (если ты используешь RAG контекст).

---

### 6) Вкладка «Логи и версии»

* История версий:

  * `v1.0 — первый релиз`
  * `v1.1 — добавлены новые диалоги`
  * `v1.2 — усилены границы по финансам`
* Для каждой:

  * Параметры обучения (датасеты, шаги, модель)
  * Кнопка `Откатиться`
* Логи:

  * Время старта, окончания, ошибки
  * График loss/metrics (простая визуализация).

---

## F. Экран «Датасеты»

Отдельный раздел, потому что ты строишь **серьёзную платформу**.

Список датасетов:

* Имя
* Тип: `Диалоги`, `Документы`, `Микс`.
* Источники: Telegram, WhatsApp, e-mail, файлы.
* Размер (сообщения, токены).
* Языки (RU, EN, MIX).
* Привязанные клоны (используется в N клонах).

Открывая конкретный датасет, пользователь видит:

1. **Обзор качества**

   * % дублей
   * % токсичного контента
   * % личных данных (оценка)
   * Статус очистки.

2. **Режим просмотра диалогов**

   * Таймлайн чатов, возможность фильтрации:

     * по участнику,
     * по теме (авто-кластеры),
     * по дате.

3. **Правила обработки (глобальные)**

   * Такие же no-code правила очистки и анонимизации.

4. **Кнопка `Использовать в новом клоне`**

   * Быстрый старт создания клона на основе этого датасета.

---

## G. Команда, роли, биллинг

### 1. Настройки команды

* Приглашение участников:

  * Роль `Owner`, `ML-инженер`, `Продакт`, `Заказчик`.
* Разграничение:

  * Кто может видеть сырые данные.
  * Кто может только смотреть ответы клона, но не видеть исходные диалоги.

### 2. Биллинг

* Тарифы:

  * По кол-ву клонов
  * По часам обучения / GPU
  * По количеству активных интеграций/нод.

---

## Что в итоге у тебя есть

1. **Лендинг**, который сразу показывает: это не конструктор ботов, а **студия цифровых двойников**.
2. **Личный кабинет** с мощным Dashboard: клоны, датасеты, обучение, деплой.
3. **Workspace клона**, где:

   * всё главное разделено по вкладкам,
   * поведение задаётся no-code,
   * есть уникальные элементы: «Карта личности», «Граф памяти», «Сценарные тесты».
4. **Чёткий путь:**

   * Загрузил диалоги → почистил / настроил память → настроил поведение → прогнал тесты → выгрузил в локальную ноду / бота.

