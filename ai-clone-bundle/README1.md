# AI Clone Bundle

Это ваш персональный AI Clone бандл. Он позволяет запускать дообученную модель локально, общаться с ней через веб-интерфейс и подключать Telegram-бота.

## 📋 Требования

Перед запуском убедитесь, что у вас есть:

*   **Python 3.10+**
*   **RAM**: Минимум 16GB (рекомендуется 24GB+ для Qwen2.5-VL-7B).
*   **GPU (Опционально)**: NVIDIA GPU с поддержкой CUDA (минимум 12GB VRAM) для быстрой работы. На CPU работа будет медленной.
*   **Интернет**: Для первого запуска требуется скачать базовую модель (~15GB).

## 🚀 Быстрый старт

### macOS / Linux

1.  **Откройте терминал** в папке с бандлом.
2.  **Запустите скрипт установки и запуска**:
    ```bash
    chmod +x run.sh
    ./run.sh
    ```
    *Скрипт автоматически создаст виртуальное окружение, установит зависимости и запустит сервер.*

### Windows

1.  **Дважды кликните** по файлу `run.bat`.
    *Откроется консоль, установятся зависимости и запустится сервер.*

### ☁️ Google Colab

В Colab нужно сначала перейти в папку с бандлом. Предположим, вы загрузили папку `ai-clone-bundle` на Google Drive.

1.  **Смонтируйте диск и перейдите в папку**:
    ```python
    from google.colab import drive
    drive.mount('/content/drive')
    %cd /content/drive/MyDrive/path/to/ai-clone-bundle
    ```

2.  **Установите зависимости**:
    ```python
    !pip install -r requirements.txt
    ```

3.  **Запустите сервер и откройте порт**:
    ```python
    import sys
    import os
    import threading
    from google.colab import output
    from google.colab.output import eval_js

    # 1. Добавляем текущую папку в sys.path, чтобы Python видел наши модули
    sys.path.append(os.getcwd())

    # 2. Импортируем приложение
    try:
        import uvicorn
        from ai_clone_server.app import app
    except Exception as e:
        print(f"КРИТИЧЕСКАЯ ОШИБКА ИМПОРТА: {e}")
        print("1. Проверьте, что вы установили зависимости (!pip install ...)")
        print("2. Проверьте, что вы в правильной папке (%cd ...)")
        raise e

    # 3. Запускаем сервер в отдельном потоке
    def run_server():
        # Убиваем старый процесс, если есть
        !fuser -k 3000/tcp
        # Запускаем uvicorn
        uvicorn.run(app, host="127.0.0.1", port=3000, log_level="info")

    threading.Thread(target=run_server, daemon=True).start()

    # 4. Открываем порт и печатаем ссылку
    proxy_url = eval_js('google.colab.kernel.proxyPort(3000)')
    print(f"Click here to open: {proxy_url}")
    ```
    *Нажмите на ссылку, которая появится в выводе (она будет вида `https://...colab.googleusercontent.com`).*
    *Если видите белый экран — попробуйте обновить страницу через пару секунд.*

---

## 🖼 Только фронтенд (для дизайна)
Чтобы не поднимать модель и сервер, можно просто отдать статические файлы веб‑панели:

```bash
./run_webui_only.sh          # по умолчанию http://127.0.0.1:4173
# или с портом
PORT=5000 ./run_webui_only.sh
```

⚠️ Это только UI-превью. Запросы к API при таком запуске не работают, он нужен для верстки/дизайна.

---

## 🛠 Ручная установка (если скрипты не работают)

Если автоматические скрипты не сработали, выполните команды вручную:

1.  **Создайте виртуальное окружение**:
    ```bash
    python -m venv .venv
    ```

2.  **Активируйте окружение**:
    *   macOS/Linux: `source .venv/bin/activate`
    *   Windows: `.venv\Scripts\activate`

3.  **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите сервер**:
    ```bash
    # macOS/Linux
    export PYTHONPATH=$PYTHONPATH:$(pwd)
    python -m uvicorn ai_clone_server.app:app --host 127.0.0.1 --port 3000

    # Windows (cmd)
    set PYTHONPATH=%PYTHONPATH%;%cd%
    python -m uvicorn ai_clone_server.app:app --host 127.0.0.1 --port 3000
    ```

---

## 🖥 Использование

После успешного запуска (в консоли появится `Uvicorn running on http://127.0.0.1:3000`), откройте браузер:

**http://localhost:3000**

### 1. Тестовый Чат (Test Chat)
Перейдите на вкладку **Test Chat**. Напишите сообщение (например, "Привет, кто ты?").
*   **Первый запрос займет время**: Сервер скачает базовую модель `Qwen/Qwen2.5-VL-7B-Instruct` с Hugging Face. Следите за прогрессом в консоли.
*   После загрузки модель ответит, используя ваш LoRA адаптер (из папки `models/lora`).

### 2. Настройка Клона (Clone Settings)
Здесь можно изменить **System Prompt**. Это инструкция, которая задает поведение модели.
*   *Пример*: "Ты — опытный программист, отвечай кратко и с кодом."

### 3. База Знаний (RAG)
Загрузите текстовые файлы (`.txt`, `.md`), чтобы клон знал специфическую информацию (например, документацию проекта).
*   При ответе клон будет искать релевантные куски в этих файлах и использовать их в ответе.
*   В бандле уже лежит предсобранный индекс RAG в `data/rag_index` (embeddings.npy + records.jsonl). Он подхватывается автоматически при старте, поэтому клон сразу умеет отвечать по этим знаниям.
*   Если загружаете новые файлы через админку, они добавятся в динамическое хранилище (Chroma). Предсобранный индекс остаётся read-only.

### 4. Telegram Бот
1.  Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram и получите токен.
2.  В админке включите галочку **Enable Telegram Bot**.
3.  Вставьте токен и нажмите **Save**.
4.  Напишите своему боту в Telegram — он ответит от лица клона.

---

## ❓ Решение проблем

| Проблема | Решение |
| --- | --- |
| **Ошибка `Out of Memory` (OOM)** | У вас не хватает оперативной или видеопамяти. Попробуйте закрыть другие тяжелые приложения. |
| **Модель не качается** | Проверьте интернет. Убедитесь, что `huggingface-cli login` не требуется (Qwen2.5-VL обычно открытая, но иногда нужен токен). |
| **Медленные ответы** | Если у вас нет GPU, модель работает на процессоре. Это нормально для моделей 7B параметров. |
| **Ошибка `ModuleNotFoundError`** | Вы не активировали виртуальное окружение или не установили `pip install -r requirements.txt`. |
